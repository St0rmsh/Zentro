/**
 * Re-export routes from router configuration
 * This ensures single source of truth for all application routes
 */
export { ROUTES, RouteAccessLevel } from "@/router/routes.config";
export type { RouteKey } from "@/router/routes.config";
